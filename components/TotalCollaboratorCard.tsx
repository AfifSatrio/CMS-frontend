import React from 'react'
import { Card, CardHeader, CardTitle, CardContent } from './ui/card'

const TotalCollaboratorCard = () => {
  return (
    <Card>
        <CardHeader>
            <CardTitle>
                Total Collaborator
            </CardTitle>
        </CardHeader>
        <CardContent>
            XX
        </CardContent>
    </Card>
  )
}

export default TotalCollaboratorCard